//import chalk from 'chalk';
let chalk = require('chalk');

console.log(chalk.bold('message') === 'message');

export default function (message, char, line, column) {

	// throw new Error(`${message}\n${line}`);
}
